services:
  database:
    image: mariadb:10.6.4-focal
    restart: unless-stopped
    container_name: "b_database"
    ports:
      - "4031:3306"
    env_file: .env
    environment:
      MYSQL_ROOT_PASSWORD: '${MYSQL_ROOT_PASSWORD}'
      MYSQL_DATABASE: '${MYSQL_DATABASE}'
      MYSQL_USER: '${MYSQL_USER}'
      MYSQL_PASSWORD: '${MYSQL_PASSWORD}'
    volumes:
      - b_db_data:/var/lib/mysql
    networks:
      - b_network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    restart: unless-stopped
    container_name: "b_phpmyadmin"
    depends_on:
      - database
    ports:
      - "4012:80"
    env_file: .env
    environment:
      PMA_HOST: database
      MYSQL_ROOT_PASSWORD: '${MYSQL_ROOT_PASSWORD}'
    networks:
      - b_network

  wordpress:
    image: wordpress:fpm
    restart: unless-stopped
    container_name: "b_wordpress"
    depends_on:
      - database
    env_file: .env
    environment:
      WORDPRESS_DB_HOST: database:3306
      WORDPRESS_DB_NAME: '${MYSQL_DATABASE}'
      WORDPRESS_DB_USER: '${MYSQL_USER}'
      WORDPRESS_DB_PASSWORD: '${MYSQL_PASSWORD}'
    volumes:
      - b_wp_data:/var/www/html
      - ./wp-content:/var/www/html/wp-content
      - ./php-conf/uploads.ini:/usr/local/etc/php/conf.d/uploads.ini
    networks:
      - b_network

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    container_name: "b_nginx"
    depends_on:
      - wordpress
    ports:
      - "4011:80"
    volumes:
      - b_wp_data:/var/www/html:ro
      - ./wp-content:/var/www/html/wp-content:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - b_network

  wpcli:
    image: wordpress:cli
    container_name: "b_wpcli"
    depends_on:
      - wordpress
      - database
    entrypoint: wp
    env_file: .env
    environment:
      WORDPRESS_DB_HOST: database:3306
      WORDPRESS_DB_NAME: '${MYSQL_DATABASE}'
      WORDPRESS_DB_USER: '${MYSQL_USER}'
      WORDPRESS_DB_PASSWORD: '${MYSQL_PASSWORD}'
    networks:
      - b_network
    volumes:
      - b_wp_data:/var/www/html
      - ./wp-content:/var/www/html/wp-content

volumes:
  b_db_data:
  b_wp_data:

networks:
  b_network:
    driver: bridge